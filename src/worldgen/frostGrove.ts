/** @deprecated use generateWorld from generator — kept for import compatibility */
export type { Pickup, PickupKind, WorldSpec } from './types';
export { generateWorld as generateFrostGrove } from './generator';
